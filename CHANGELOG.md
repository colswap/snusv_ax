# 변경 이력

무엇이 바뀌었는지 적는다. 왜 그렇게 정했는지는 [docs/adr/](docs/adr/)에 있다.

## [미배포]

### 추가
- `finance_reminder` 자동화. 회계 채널에서 ✅가 안 달린 정산 요청을 매일 10:07 KST에 회계 담당자에게 요약 DM한다. 미처리가 0건이면 보내지 않는다.
- `shared/` 공통 모듈 — 환경변수 검증(`config`), Slack 호출·재시도(`slack`), User Group 조회 및 DM 발송(`dm`). 앞으로 추가될 자동화가 공유한다.
- `keepalive.yml` — 퍼블릭 레포의 스케줄 워크플로가 60일 비활성화로 꺼지는 것을 막는 월 1회 자동 커밋.
- 인수인계 문서 — `README`, `RUNBOOK`, ADR 4건, 설계 스펙.
