[project]
name = "snusv-ax"
version = "0.1.0"
description = "SNUSV 운영 자동화 모음"
requires-python = ">=3.11"
dependencies = [
    "slack-sdk>=3.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
